import React from 'react';

const Paginado = (props) => {

  // var [cantPag, setCantPag] = React.useState([])
  
  // React.useEffect(()=>{

  //   for(var i = 0; i < props.pagina.length; i++){
  //     cantPag.push(i)
  //   }
  // })

  // console.log(cantPag)

  // {
  //     cantPag.map((el,index)=>{
  //       return <button key={index}>{el}</button>
  //     })
  //    }

  
  return (
    <div className="paginado">
     
    </div>
  )
}

export default Paginado;